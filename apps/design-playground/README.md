# Graphics App

This is a graphics generation tool built using the primitives from `@design-system/ui`.

## Features
- Create customizable cards with titles and descriptions.
- Change background colors.
- Uses a shared UI component library.

## Development
Run `pnpm dev` to start the development server.
Run `pnpm build` to build for production.
